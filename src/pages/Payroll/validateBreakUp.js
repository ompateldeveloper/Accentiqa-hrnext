export const validateBreakUp= (values) => {
    const errors = {};
    //Validation For name
    // if (!values.name.trim()) {
    //   errors.name = "Name is required";
    // } else if (values.name.length < 3) {
    //   errors.name = "Name must be at least 3 characters long";
    // }
  
    return errors;
  };